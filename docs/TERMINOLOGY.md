# Terminology

- **ads-optimizer** — Claude Code skill that analyzes Facebook Ads performance via the `meta-ads` MCP server and recommends budget, pause, and scaling actions. Config lives in `.claude/ads-agent/`.
