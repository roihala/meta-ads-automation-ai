apiVersion: v1
kind: Service
metadata:
  name: campaigner-web
  labels:
    app: campaigner-web
spec:
  type: ClusterIP
  selector:
    app: campaigner-web
  ports:
    - name: http
      port: 80
      targetPort: http
