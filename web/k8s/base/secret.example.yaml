# Example only — real Secret is provisioned via External Secrets Operator or sealed-secrets.
# Keys expected by the runtime:
#   DATABASE_URL   (required for WEB_DB_MODE=local-postgres)
#   BUSINESS_ID    (required)
#   NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY (only when WEB_DB_MODE=supabase)
apiVersion: v1
kind: Secret
metadata:
  name: campaigner-web-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql://REPLACE:REPLACE@REPLACE:5432/REPLACE"
  BUSINESS_ID: "REPLACE-UUID"
